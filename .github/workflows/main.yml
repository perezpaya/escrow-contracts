name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/foundry-rs/foundry:latest foundry:latest
    steps:
      - uses: actions/checkout@v3
      - name: ls
        run: ls
      - name: forge
        run: forge -h

# docker run -v $PWD:/app foundry "forge test --root /app --watch"
